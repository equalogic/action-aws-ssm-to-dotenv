import { setCredentialFeature } from '../../core/dist-es/submodules/client/setCredentialFeature.js';

const isProcessProfile = (arg) => Boolean(arg) && typeof arg === "object" && typeof arg.credential_process === "string";
const resolveProcessCredentials = async (options, profile) => import('../../credential-provider-process/dist-es/index.js').then(({ fromProcess }) => fromProcess({
    ...options,
    profile,
})().then((creds) => setCredentialFeature(creds, "CREDENTIALS_PROFILE_PROCESS", "v")));

export { isProcessProfile, resolveProcessCredentials };
//# sourceMappingURL=resolveProcessCredentials.js.map
