import { awsEndpointFunctions } from '../../../../../util-endpoints/dist-es/aws.js';
import { EndpointCache } from '../../../../../../@smithy/util-endpoints/dist-es/cache/EndpointCache.js';
import { customEndpointFunctions } from '../../../../../../@smithy/util-endpoints/dist-es/utils/customEndpointFunctions.js';
import { resolveEndpoint } from '../../../../../../@smithy/util-endpoints/dist-es/resolveEndpoint.js';
import { ruleSet } from './ruleset.js';

const cache = new EndpointCache({
    size: 50,
    params: ["Endpoint", "Region", "UseDualStack", "UseFIPS"],
});
const defaultEndpointResolver = (endpointParams, context = {}) => {
    return cache.get(endpointParams, () => resolveEndpoint(ruleSet, {
        endpointParams: endpointParams,
        logger: context.logger,
    }));
};
customEndpointFunctions.aws = awsEndpointFunctions;

export { defaultEndpointResolver };
//# sourceMappingURL=endpointResolver.js.map
