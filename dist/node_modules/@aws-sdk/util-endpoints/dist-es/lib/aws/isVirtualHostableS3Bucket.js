import { isIpAddress } from '../../../../../@smithy/util-endpoints/dist-es/lib/isIpAddress.js';
import { isValidHostLabel } from '../../../../../@smithy/util-endpoints/dist-es/lib/isValidHostLabel.js';
import '../../../../../@smithy/util-endpoints/dist-es/lib/parseURL.js';

const isVirtualHostableS3Bucket = (value, allowSubDomains = false) => {
    if (allowSubDomains) {
        for (const label of value.split(".")) {
            if (!isVirtualHostableS3Bucket(label)) {
                return false;
            }
        }
        return true;
    }
    if (!isValidHostLabel(value)) {
        return false;
    }
    if (value.length < 3 || value.length > 63) {
        return false;
    }
    if (value !== value.toLowerCase()) {
        return false;
    }
    if (isIpAddress(value)) {
        return false;
    }
    return true;
};

export { isVirtualHostableS3Bucket };
//# sourceMappingURL=isVirtualHostableS3Bucket.js.map
