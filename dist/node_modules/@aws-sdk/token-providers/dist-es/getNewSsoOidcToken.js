import { getSsoOidcClient } from './getSsoOidcClient.js';

const getNewSsoOidcToken = async (ssoToken, ssoRegion, init = {}) => {
    const { CreateTokenCommand } = await import('../../nested-clients/dist-es/submodules/sso-oidc/index.js');
    const ssoOidcClient = await getSsoOidcClient(ssoRegion, init);
    return ssoOidcClient.send(new CreateTokenCommand({
        clientId: ssoToken.clientId,
        clientSecret: ssoToken.clientSecret,
        refreshToken: ssoToken.refreshToken,
        grantType: "refresh_token",
    }));
};

export { getNewSsoOidcToken };
//# sourceMappingURL=getNewSsoOidcToken.js.map
