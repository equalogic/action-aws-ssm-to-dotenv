import { booleanSelector } from '../../../util-config-provider/dist-es/booleanSelector.js';
import { SelectorType } from '../../../util-config-provider/dist-es/types.js';

const ENV_USE_FIPS_ENDPOINT = "AWS_USE_FIPS_ENDPOINT";
const CONFIG_USE_FIPS_ENDPOINT = "use_fips_endpoint";
const NODE_USE_FIPS_ENDPOINT_CONFIG_OPTIONS = {
    environmentVariableSelector: (env) => booleanSelector(env, ENV_USE_FIPS_ENDPOINT, SelectorType.ENV),
    configFileSelector: (profile) => booleanSelector(profile, CONFIG_USE_FIPS_ENDPOINT, SelectorType.CONFIG),
    default: false,
};

export { CONFIG_USE_FIPS_ENDPOINT, ENV_USE_FIPS_ENDPOINT, NODE_USE_FIPS_ENDPOINT_CONFIG_OPTIONS };
//# sourceMappingURL=NodeUseFipsEndpointConfigOptions.js.map
