import { fromBase64 } from '../../../util-base64/dist-es/fromBase64.js';
import { toBase64 } from '../../../util-base64/dist-es/toBase64.js';
import { fromUtf8 } from '../../../util-utf8/dist-es/fromUtf8.js';
import { toUtf8 } from '../../../util-utf8/dist-es/toUtf8.js';
import { Uint8ArrayBlobAdapter } from './Uint8ArrayBlobAdapter.js';

function transformToString(payload, encoding = "utf-8") {
    if (encoding === "base64") {
        return toBase64(payload);
    }
    return toUtf8(payload);
}
function transformFromString(str, encoding) {
    if (encoding === "base64") {
        return Uint8ArrayBlobAdapter.mutate(fromBase64(str));
    }
    return Uint8ArrayBlobAdapter.mutate(fromUtf8(str));
}

export { transformFromString, transformToString };
//# sourceMappingURL=transforms.js.map
