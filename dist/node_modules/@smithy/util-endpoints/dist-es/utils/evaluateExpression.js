import { EndpointError } from '../types/EndpointError.js';
import { callFunction } from './callFunction.js';
import { evaluateTemplate } from './evaluateTemplate.js';
import { getReferenceValue } from './getReferenceValue.js';

const evaluateExpression = (obj, keyName, options) => {
    if (typeof obj === "string") {
        return evaluateTemplate(obj, options);
    }
    else if (obj["fn"]) {
        return callFunction(obj, options);
    }
    else if (obj["ref"]) {
        return getReferenceValue(obj, options);
    }
    throw new EndpointError(`'${keyName}': ${String(obj)} is not a string, function or reference.`);
};

export { evaluateExpression };
//# sourceMappingURL=evaluateExpression.js.map
