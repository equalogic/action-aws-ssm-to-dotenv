import { getChecksumConfiguration, resolveChecksumRuntimeConfig } from './checksum.js';
import { getRetryConfiguration, resolveRetryRuntimeConfig } from './retry.js';

const getDefaultExtensionConfiguration = (runtimeConfig) => {
    return Object.assign(getChecksumConfiguration(runtimeConfig), getRetryConfiguration(runtimeConfig));
};
const resolveDefaultRuntimeConfig = (config) => {
    return Object.assign(resolveChecksumRuntimeConfig(config), resolveRetryRuntimeConfig(config));
};

export { getDefaultExtensionConfiguration, resolveDefaultRuntimeConfig };
//# sourceMappingURL=defaultExtensionConfiguration.js.map
