import { chain } from '../../property-provider/dist-es/chain.js';
import { memoize } from '../../property-provider/dist-es/memoize.js';
import { fromEnv } from './fromEnv.js';
import { fromSharedConfigFiles } from './fromSharedConfigFiles.js';
import { fromStatic } from './fromStatic.js';

const loadConfig = ({ environmentVariableSelector, configFileSelector, default: defaultValue }, configuration = {}) => {
    const { signingName, logger } = configuration;
    const envOptions = { signingName, logger };
    return memoize(chain(fromEnv(environmentVariableSelector, envOptions), fromSharedConfigFiles(configFileSelector, configuration), fromStatic(defaultValue)));
};

export { loadConfig };
//# sourceMappingURL=configLoader.js.map
