import { getConfigFilepath } from './getConfigFilepath.js';
import { getSsoSessionData } from './getSsoSessionData.js';
import { parseIni } from './parseIni.js';
import { slurpFile } from './slurpFile.js';

const swallowError = () => ({});
const loadSsoSessionData = async (init = {}) => slurpFile(init.configFilepath ?? getConfigFilepath())
    .then(parseIni)
    .then(getSsoSessionData)
    .catch(swallowError);

export { loadSsoSessionData };
//# sourceMappingURL=loadSsoSessionData.js.map
